module.exports={
    GENERAL:{
        required_field:"The field {PATH} is required",
        minlength:"The field {PATH} must have at least {VALUE}",
        maxlength:"The field {PATH} can´t have more than {VALUE}"
    },
    USERSADMIN:{
        userExist:"The user {VALUE} already exists",
        passwordNumberRequired:"The {PATH} must contains at least one number"
    }
}